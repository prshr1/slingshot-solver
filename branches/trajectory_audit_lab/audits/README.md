Audit workspace:

- `generated/`: outputs created by branch scripts/notebooks
- `history/`: relocated historical audit outputs from `results/audit_*`, plus legacy `figures/` and `frames/`
- `reports/`: relocation manifests and audit summaries
