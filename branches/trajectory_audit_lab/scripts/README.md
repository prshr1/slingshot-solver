Branch-local scripts:

- `trajectory_gradient_renderer.py`: render star/planet/combined static and video trajectory gradients
- `relocate_experiment_outputs.py`: relocate existing audit/smoke/temp result directories into this lab
